{{#view Vine.VideoBoxView}}
	<div class="video">
		<video loop preload="auto" class="centered" {{bindAttr src="url"}}></video>
		
		<div class="centered tags">
			{{#each tag in video.tags}}
				{{tag.name}}
			{{/each}}
		</div>
	</div>
{{/view}}
